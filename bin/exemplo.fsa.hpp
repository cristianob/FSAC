/**
 * This code is autogenerated by FSAC.
 * Any modifications may be discarted by a future compilation of the FSA file, please
 * modify the FSA source file and recompile it.
 */
#ifndef EXEMPLO_H_
#define EXEMPLO_H_

#include "Classes/Fieldstate.h"
#include "Classes/Robot.h"

#define SEARCHING_BALL 0
#define ALIGN_BALL 1
#define RUNNING_GOAL 2

#define distance_robot_ball 0
#define angle_robot_ball_goal 1

class GameStateFSA {
	public:
	GameStateFSA();
	~GameStateFSA();
	void setStateFunction(uint8_t state_i, bool (*fn)(Robot *robot, Fieldstate *fs));
	void setInput(uint8_t input_name, bool (*fn)(Robot *robot, Fieldstate *fs));
	bool callInput(uint8_t input_name);
	uint8_t calculateState();
	void runStateFunction();

	private:
	uint8_t state;
	bool (*inputs)(Robot *robot, Fieldstate *fs)[2];
	bool (*state_function)(Robot *robot, Fieldstate *fs)[3];
}

#endif